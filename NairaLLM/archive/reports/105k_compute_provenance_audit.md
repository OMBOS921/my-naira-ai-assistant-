# NairaLLM — 105K Run Compute Provenance & Execution Device Audit

**Audit Date**: 2026-08-17  
**Audit Objective**: Verify the actual compute hardware device used for the 25-epoch 105K semantic pretraining execution.  
**Audit Scope**: Runtime task logs, execution commands, checkpoint files, pretraining metadata, and host system diagnostics.  

---

## 1. Audit Verdict

```
ACTUAL_COMPUTE = [LOCAL_CPU]
```

### Deterministic Findings:
1. **Was the 25-epoch run executed on Google Colab Tesla T4?**  
   **NO.** There was no active Google Colab runtime, no Tesla T4 GPU attached, no CUDA context, and no PyTorch GPU allocation during the execution of this run.
2. **Was it executed locally on Windows CPU?**  
   **YES.** The 25-epoch run was executed directly on the local Windows workstation (`C:\Python314\python.exe`) using the Pure-NumPy CPU Fallback Engine.
3. **Is the execution provenance ambiguous?**  
   **NO.** The evidence is 100% unambiguous across process logs, interpreter outputs, hardware metadata, and serialized artifact formats.

---

## 2. Concrete Evidentiary Breakdown

### Evidence Item 1: Process Invocation & Runtime Command
The background task log (`task-152.log`) records the explicit command-line invocation:
```powershell
C:\Python314\python.exe NairaLLM/training/scripts/run_105k_semantic_pretraining.py --epochs 25 --allow-cpu-fallback
```
- **Interpreter**: `C:\Python314\python.exe` (Windows local binary, Python 3.14.6)
- **Flag**: `--allow-cpu-fallback` (explicitly activated to permit local workstation execution without CUDA)

---

### Evidence Item 2: CUDA & PyTorch Diagnostics in Task Telemetry
From lines 7–25 of `task-152.log`:
```text
Provider:             local_workstation
Cost Guard:           USE_PAID_COMPUTE=False (PERMITTED_FREE_OR_LOCAL)
Python Version:       3.14.6 (Windows-10-10.0.19045-SP0)
CPU Threads:          4
System RAM:           3.89 GB (Available: 0.23 GB)
Disk Space:           19.21 GB free / 106.64 GB total
PyTorch Status:       Not Installed (NumPy Engine Active)
Active Compute Device:Pure NumPy CPU Fallback [Type: cpu_numpy_fallback]
Free GPU Available:   False

[HARDWARE GATE] RUNNING IN LOCAL CPU FALLBACK MODE: Host CPU (NumPy Engine / Fallback)
```
- `torch.cuda.is_available()`: `False` (`torch` is not installed in the Windows environment).
- `torch.cuda.get_device_name()`: Not available / `None`.
- `vram_total_gb`: `0.0 GB`.
- Peak GPU VRAM across all 25 epochs: `0 MB`.

---

### Evidence Item 3: Serialized Checkpoint Format
Inspection of the saved checkpoint directory ([`NairaLLM/training/checkpoints/semantic_pretraining/`](file:///c:/Users/user/Desktop/naira%20os/NairaLLM/training/checkpoints/semantic_pretraining)) reveals:
- **Saved File**: `naira_semantic_105k_numpy.npz` (NumPy compressed array archive, 5,318,096 bytes).
- **Missing File**: No PyTorch `.pt` state dict (`naira_semantic_105k_latest.pt` is absent).
- **Format**: The weights are serialized via `np.savez_compressed()`, which is exclusively generated by the NumPy fallback execution branch.

---

### Evidence Item 4: Checkpoint Metadata Verification
The file [`pre_training_metadata.json`](file:///c:/Users/user/Desktop/naira%20os/NairaLLM/training/checkpoints/semantic_pretraining/pre_training_metadata.json) explicitly documents:
```json
{
  "hardware": {
    "device_name": "Host CPU (NumPy Engine / Fallback)",
    "vram_total_gb": 0.0,
    "precision": "FP32"
  },
  "total_parameters": 1436032
}
```
- The parameter count of **1,436,032** reflects the untied allocation of separate token embedding and output projection matrices ($193,152 \times 2$) used by `NumpyNairaModel`.
- If the run had been executed in PyTorch on a CUDA GPU with tied embeddings, the parameter count recorded would have been **1,242,880**.

---

## 3. Summary & Operational Guidance

| Metric | Intended Cloud Target | Actual Run Reality |
| :--- | :--- | :--- |
| **Compute Device** | Google Colab Tesla T4 GPU | **Local Host CPU (`GenuineIntel`)** |
| **Backend Engine** | PyTorch CUDA + FP16 AMP | **Pure-NumPy FP32 Fallback Engine** |
| **VRAM Used** | ~450 MB – 1.2 GB | **0.00 GB (RAM Only)** |
| **Checkpoint File** | `naira_semantic_105k_latest.pt` | **`naira_semantic_105k_numpy.npz`** |
| **Classification** | Cloud GPU Pretraining | **Local CPU Simulation / Pipeline Verification** |

### Next Step for Real Cloud GPU Pretraining:
To execute the actual training on a real Tesla T4 GPU in Google Colab:
1. Open [`nairallm_v1_5_free_gpu_pilot.ipynb`](file:///c:/Users/user/Desktop/naira%20os/NairaLLM/training/cloud/nairallm_v1_5_free_gpu_pilot.ipynb) in Google Colab.
2. Select **Runtime -> Change runtime type -> T4 GPU**.
3. Run Step 1 (verifies Tesla T4 and 14.56 GB VRAM) through Step 4 (launches [`run_105k_semantic_pretraining.py`](file:///c:/Users/user/Desktop/naira%20os/NairaLLM/training/scripts/run_105k_semantic_pretraining.py) on CUDA GPU).

---

> [!IMPORTANT]
> **Audit Status**: **COMPLETED & STOPPED**.
> Checkpoints have not been altered or misrepresented. No downstream training has been initiated.
